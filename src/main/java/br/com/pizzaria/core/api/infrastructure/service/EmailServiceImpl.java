package br.com.pizzaria.core.api.infrastructure.service;

import org.springframework.stereotype.Service;

//import br.com.autenticacao.api.presentation.dto.email.EmailResponseTO;

@Service
public class EmailServiceImpl {

   //@Autowired
   // private RabbitTemplate rabbitTemplate;

	/*
	 * public void send(EmailResponseTO email) {
	 * //rabbitTemplate.convertAndSend(ApplicationProperty.QUEUE_EMAIL_NAME, email);
	 * }
	 */

}
